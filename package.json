{
  "name": "Medicare-root",
  "version": "1.0.0",
  "description": "Root control for Medicare Booking Application",
  "type": "module",
  "scripts": {
    "install:all":  "npm install --prefix backend && npm install --prefix frontend",
    "build":        "npm --prefix frontend run build",
    "render-build": "npm install --prefix backend && npm install --include=dev --prefix frontend && npm --prefix frontend run build",
    "start":        "node backend/server.js",
    "dev":          "node start.js"
  }
}